# Step 06 — checkpoints cases

## Goal
Parity cases for `checkpoints`.

## Cases
- `checkpoints`, `checkpoints --json`.
- `checkpoints --repo <r>`.
- `checkpoints --limit 3 --days 14`.

## Done when
- [ ] All cases pass.
