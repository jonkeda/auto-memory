# Step 04 — Create AutoMemory.Tests xunit

## Goal
Create the xUnit test project mirroring `src/session_recall/tests/`.

## Commands
```powershell
dotnet new xunit -o net/tests/AutoMemory.Tests
```

## Done when
- [ ] `net/tests/AutoMemory.Tests/AutoMemory.Tests.csproj` exists.
- [ ] Default `UnitTest1` test passes via `dotnet test`.
