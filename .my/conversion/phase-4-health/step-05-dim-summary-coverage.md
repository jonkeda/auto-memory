# Step 05 — DimSummaryCoverage

## Goal
Port `health/dim_summary_coverage.py` → `Health/DimSummaryCoverage.cs`.

## Notes
- Ratio of sessions with non-empty `summary` over total.
- Same null/empty treatment as Python.

## Done when
- [ ] Ratio rounding matches Python (`round(x, 2)`).
