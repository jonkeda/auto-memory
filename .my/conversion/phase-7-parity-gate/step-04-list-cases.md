# Step 04 — list cases

## Goal
Author parity cases for `list`.

## Cases
- `list` (default).
- `list --json`.
- `list --repo <r>`.
- `list --limit 1`.
- `list --days 7`.
- `list` against empty fixture.

## Done when
- [ ] All cases pass byte-identical against fixture DB.
