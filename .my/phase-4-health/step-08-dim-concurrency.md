# Step 08 — DimConcurrency

## Goal
Port `health/dim_concurrency.py` → `Health/DimConcurrency.cs`.

## Notes
- Probes WAL state, busy retries, etc. Verify what Python actually measures and replicate.

## Done when
- [ ] Score + detail match Python on fixture.
