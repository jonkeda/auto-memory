# Step 03 — DimCorpus

## Goal
Port `health/dim_corpus.py` → `Health/DimCorpus.cs`.

## Notes
- Probes total session count, file count, etc.
- Score thresholds + bucket boundaries: copy from Python verbatim.
- Detail dict field names match Python.

## Done when
- [ ] Per-bucket assertions match Python on seeded fixture.
