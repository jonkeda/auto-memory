# Step 09 — DimLatency

## Goal
Port `health/dim_latency.py` → `Health/DimLatency.cs`.

## Notes
- Reads recent telemetry entries (or runs a probe) — copy Python's data source choice.
- Bucket thresholds verbatim.

## Done when
- [ ] Score + detail match Python on a controlled telemetry fixture.
